{
  "name": "blog-platform",
  "version": "1.0.0",
  "description": "Blog Platform with Comments — user auth, post CRUD, comments, MySQL.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mysql2": "^3.10.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=16"
  }
}
